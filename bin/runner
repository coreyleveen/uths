#!/usr/bin/env ruby

require "bundler/setup"
require "pry"
require "uths"

File.delete("log") if File.file?("log")

player = Player.new(chips: 5_000)

1000.times { Round.new(player: player).play }

puts File.read("log")
